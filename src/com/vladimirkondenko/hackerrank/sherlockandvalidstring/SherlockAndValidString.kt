package com.vladimirkondenko.hackerrank.sherlockandvalidstring

fun main(args: Array<String>) {
/* Enter your code here. Read input from STDIN. Print output to STDOUT. */
}

fun isValid(s: String): Boolean {
    /*
    val chars = HashMap<String, Integer>()
    for (ch in s) {
        if (ch.keys.contains(ch)) chars.put(ch, ch.get(ch) + 1)
        else chars.put(ch, 1)
    }
    val firstNum = chars.values.iterator().next()
    for (ch in chars) {
        val num = chars.get(ch)
        if (num != firstNum) {
            // deletion
        }
    }
    */
    return false
}